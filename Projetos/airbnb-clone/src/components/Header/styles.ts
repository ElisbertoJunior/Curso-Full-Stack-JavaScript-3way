import styled from "styled-components";

export const Header = styled.header`
  background-color: #FFFFFF;
  -webkit-box-shadow: -5px 6px 19px 4px rgba(0,0,0,0.09);
-moz-box-shadow: -5px 6px 19px 4px rgba(0,0,0,0.09);
box-shadow: -5px 6px 19px 4px rgba(0,0,0,0.09);
`
export const Container = styled.div`
  max-width: 1024px;
  width: 100%;
  padding: 22px 30px;
  margin: 0 auto;
`
export const Img = styled.img`
  height: 25px;
`